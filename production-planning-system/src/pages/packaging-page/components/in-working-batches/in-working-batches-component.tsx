// import { useBatches } from "../../../../services/hooks/useBatches"

// const InWorkingBatchesComponent: React.FC = () => {
//   const {data: batches, isLoading, isSuccess} = useBatches();

//   if (isLoading) {
//     return <span>Loading...</span>
//   }

//   if (isSuccess) {
//     const notComletedAndCurrentLine = batches.filter((batch) => batch.isBatchCompletedSap === false && batch.packagingBatchDetails!.length > 0)
//     const lines = 

//     return(

//     )
//   }
// }